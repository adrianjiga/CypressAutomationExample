name: Run Tests

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test - ${{ matrix.group.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        group: 
          - name: api
            tag: '@api'
          - name: ui
            tag: '@ui'
          - name: webtables
            tag: '@webTables'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Run ${{ matrix.group.name }} Tests
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
          env: grepTags=${{ matrix.group.tag }}

      - name: Upload Test Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-results-${{ matrix.group.name }}
          path: |
            cypress/screenshots/
            cypress/videos/
            reports/${{ matrix.group.name }}/
          if-no-files-found: ignore
          retention-days: 30