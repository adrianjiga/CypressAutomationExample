x-cypress-service: &cypress-service
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - .:/app
    - /app/node_modules
  deploy:
    resources:
      limits:
        memory: 2G
      reservations:
        memory: 1G
  environment:
    - CI=true

services:
  cypress-tests:
    <<: *cypress-service
    command: npx cypress run

  ui-tests:
    <<: *cypress-service
    command: sh -c "GREP_TAGS=@ui npx cypress run"

  api-tests:
    <<: *cypress-service
    command: sh -c "GREP_TAGS=@api npx cypress run"

  webtables-tests:
    <<: *cypress-service
    command: sh -c "GREP_TAGS=@webTables npx cypress run"
